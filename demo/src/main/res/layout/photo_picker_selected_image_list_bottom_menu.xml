<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/scan_selected_image_thumbnial_bottom_menu"
    android:layout_width="match_parent"
    android:layout_height="48dp"
    android:layout_alignParentBottom="true"
    android:background="@color/photo_picker_color_greyest_transparent"
    android:gravity="center_vertical">

    <Button
        android:id="@+id/scan_selected_image_thumbnial_confirm_btn"
        android:layout_width="wrap_content"
        android:layout_height="48dp"
        android:layout_alignParentRight="true"
        android:background="@drawable/back_button_default_style"
        android:paddingLeft="12dp"
        android:paddingRight="12dp"
        android:text="确定"
        android:textColor="@color/photo_picker_color_white"
        android:textSize="@dimen/photo_picker_textview_smiddle_size" />

</RelativeLayout>